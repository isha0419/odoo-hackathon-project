Read the attached design.md in full before planning anything — it is the frozen source of truth. Produce an implementation plan for 4 developers + 1 integration lead that follows Section 12 exactly: the four tracks (A: Auth+Org, B: Assets+Allocation+Transfer, C: Booking+Maintenance, D: Audit+Reports+Notifications+Dashboard) and the integration lead's ownership of main.
Hard rule — zero merge conflicts: every task must edit only files inside its own track's disjoint ownership set (routers/<module>.py, services/<module>.py, schemas/<module>.py, and its own models/*.py). No two developers may touch the same file. The only shared files — main.py, db.py, deps.py, config.py, and the unified Alembic migration — belong solely to the integration lead and are written in the step-1 skeleton phase before tracks branch. Do not schedule any track to modify them afterward.
Sequencing: enforce the step-1 dependency gate — the skeleton (full migration for every table/enum/constraint in Section 3, deps.py guards, empty registered routers, and the frozen signatures for activity_service.log() and notifications_service.create()) must land on main before any feature task starts. All feature tasks then build against the contracts in Sections 6–11, not against each other's running code.
Cautions — do not: invent tables, endpoints, enums, or status transitions not in design.md; move a file between tracks; use RLS, Supabase, or any BaaS; hardcode seed timestamps; or bypass the DB-level conflict constraints. If the design is ambiguous, flag it in the plan — do not silently resolve it.
Output: per-developer task lists with explicit file paths, dependency order, and the git branch/merge protocol from Section 12.
